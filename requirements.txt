numpy>=1.26
matplotlib>=3.8
pytest>=8.0

# Ficha de campo da composição (E.4D) — composicao/fontes.py lê o YAML que o
# especialista preenche. NÃO é dependência de curadoria: `composicao/` é pacote
# de aplicação e o YAML é o formato recomendado ao usuário, então a dependência
# vive na fronteira que o pacote realmente consome.
pyyaml>=6.0
